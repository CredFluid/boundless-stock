[profile.default]
src = "src"
out = "out"
libs = ["node_modules", "lib"]
test = "test"
script = "script"
solc = "0.8.22"
optimizer = true
optimizer_runs = 200
via_ir = false
evm_version = "paris"
fs_permissions = [
  { access = "read", path = "./out" },
  { access = "read", path = "./node_modules" },
  { access = "read-write", path = "./deployments" },
  { access = "read", path = "./config" }
]

[invariant]
runs = 48
# Deep enough that every run reliably reaches a fill, a refund AND a stalled compose.
# At depth 64 with seven handler actions, trades were sparse enough that some runs never
# filled and the coverage assertions tripped.
depth = 160
fail_on_revert = false
call_override = false
shrink_run_limit = 2000

[fuzz]
runs = 512

[fmt]
line_length = 110
tab_width = 4
bracket_spacing = true
