node_modules/
cache/
out/
broadcast/
.env
.env.*
!.env.example
*.log
.DS_Store
deployments/*.local.json
# NOTE: deployments/*.manifest.json IS tracked -- it is module 6's deliverable
# and the record of a real deployment run.
.localnet/
.localnet-solana/

# Local caches written by the infra and the web app (trade history).
.crossstock/

# The boundless-stock command: logs, generated configs, the current deployment.
.boundless/
deployments/boundless-*
